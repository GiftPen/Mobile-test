# 스타 러시 (Star Rush) — 설계 문서

> Fruit Star의 두 번째 모드. **로그라이트 스코어링 런**.
> 레퍼런스: Balatro(족보→조커→앤티 퀘터), Cloverpit(슬롯 확률·배당 조작으로 마감 내 목표액).
> 코어 보드 규칙은 **아케이드와 100% 동일** (같은 색 인접에 놓아 3×3 팡 → 연쇄). 그 위에 **점수·경제·유물** 레이어를 얹는다.
>
> ⚠️ 이 문서의 모든 수치는 **시작값(초안)** 이며, 플레이 감으로 반복 튜닝 대상.

---

## 1. 한 줄 컨셉
"확률을 조작하는 유물을 모아, **정해진 터치 수 안에 목표 점수**를 달성하며 스테이지를 밟아나가는 런."

---

## 2. 런 구조 (한 판의 흐름)

```
런 시작
 └ 스테이지 1  ── 목표 X₁점을 터치 T회 안에 달성?
      ├ 실패 → 런 종료 (기록/보상 정산)
      └ 성공 → 코인 정산 → 상점(유물 구매/리롤) → 스테이지 2 ...
 └ 스테이지 2  ── 목표 X₂점 (X₁보다 큼) ...
 ...
 (난이도 단계 상승마다 특성 선택지 1/3 제공)
```

- **스테이지 = Balatro의 앤티/블라인드**. 넘을 때마다 목표 점수↑, 보상↑.
- **터치 수 = 핵심 자원** (Balatro의 핸드 수). 유물로 늘림.
- 언제 끝나나: 무한 스테이지(점점 어려워짐)로 "얼마나 멀리?" 하이스코어형. (또는 N스테이지 클리어 = 클리어형. → **미정, 아래 오픈 이슈**)

---

## 3. 점수 시스템

아케이드와 **분리**된 계산식. "쌓아서 큰 수" 나오게 곱연산 위주.

### 3-1. 과일별 기본점수 + 배율 (rarity=기본점수)
각 과일은 **기본점수(다름)** 와 **배율(모두 기본 1.0)** 을 별도로 가짐. 유물은 **둘 다** 건드릴 수 있음.
| 과일 | 기본점수(초안) | 배율(기본) | 스폰 확률(초안) |
|---|---|---|---|
| 🍒 체리 | 1점 | ×1.0 | 20% |
| 🍋 레몬 | 1점 | ×1.0 | 20% |
| 🍊 오렌지 | 2점 | ×1.0 | 15% |
| 🥝 키위 | 2점 | ×1.0 | 15% |
| 🍇 포도 | 3점 | ×1.0 | 12% |
| 🍑 복숭아 | 3점 | ×1.0 | 12% |
| 🍌 바나나 | 5점 | ×1.0 | 6% |

> 희귀=고점수. 희귀 과일은 잘 안 뭉쳐서 팡 내기 어려움 → **유물로 확률/점수/배율을 올려 "그 과일 빌드"를 짜는 최적화**가 핵심 재미. (밸런스 주의: 유물로 달성 가능해야 함)
> ⚠️ **BASE를 10으로 모든 과일 동일하게 두지 않는다** — 과일마다 기본점수가 달라 정체성이 생김.

### 3-2. 한 번 팡의 점수 (초안)
```
팝점수 = (팝개수 × 과일기본점수[색])          ← 색마다 기본점수 다름 (1/2/3/5)
       × 과일배율[색]                          ← 기본 1.0, 유물로 상승
       × 연쇄크기 보너스(예: 1 + (팝개수-1)*0.1)
       × 콤보/스트릭 배율(연속 성공)
       × 유물 보정(합/곱)
       + 영역 보너스(그 칸이 보너스 존이면)      ← 판 영역 유물
```
- 아케이드의 "연쇄크기·콤보" 감각을 유지하되, **과일 기본점수 + 배율**이 곱해져 고점수 과일을 크게 터뜨릴수록 폭발적.
- 유물이 이 식의 각 항(**과일별 기본점수, 배율, 연쇄보너스, 콤보, 곱셈, 영역보너스**)에 개입.
- **판 영역 보너스**: 특정 칸/영역(중앙·모서리·랜덤 하이라이트 등)에서 팡하면 추가 점수 → 유물로 부여. (구현 확인됨 ✅)

---

## 4. 자원 (Resources)

| 자원 | 역할 | 늘리는 법 |
|---|---|---|
| **터치(Touch)** | 스테이지당 행동 횟수. 0 되고 목표 미달 → 실패 | 유물 "+최대 터치", 스테이지 보상 |
| **코인(Coin)** | 상점 구매력. **점수와 별개** | 스테이지 클리어 정산 **+ 팝으로 드랍(동전 오브젝트)** |
| **리롤(Reroll)** | 우상단 다음 과일 다시 뽑기 | 유물, 상점, (광고?) |

> **점수 ↔ 코인 분리 원칙**: 점수는 목표 달성용, 코인은 빌드 구매용. 안 그러면 "점수 쓰면 목표 미달" 딜레마가 꼬임. (발라트로도 분리)

### 4-1. 동전(Coin) 오브젝트 — 결정: 도입
- 보드에 **동전 오브젝트**를 추가 (grid에 코인 타입 or 별도 `coin[r][c]` 레이어). 팡/수집으로 코인 획득.
- **아케이드 모드에서도** 동전을 재화로 써서 아이템을 살 수 있게 하는 방안 검토 중 (두 모드 공용 재화).
- 동전 도입 시 **동전 특화 유물**(동전 드랍↑, 동전 팡 시 점수↑ 등)이 자연스럽게 생김 → 빌드 다양성↑.

---

## 5. 확률 시스템 (조작 대상)

이미 코드에 조작 표면이 2개 있음:
1. **보드 스폰 색 확률** — 매 터치 시 새로 생기는 과일
2. **우상단 큐 색 확률** — 앞으로 놓을 과일 (now + 다음)

구현 기반: `colorWeight[7]` 가중치 배열 + `pickColor()` (지금의 `randInt(activeColors())` 대체). 기본 weight=1이면 **현재와 동작 100% 동일**. 유물/특성은 이 weight만 바꾸면 확률이 즉시 반영.

추가 조작 레버(유물로): 스폰 개수, 리롤, 특정 색 봉인(weight=0), 큐 별도 가중치 등.

---

## 6. 상점 (스테이지 사이)

- 클리어 → 코인 정산 → **유물 3~5개 진열** 중 구매 + 리롤(코인 소모).
- 유물 슬롯 상한(예: 5칸) → 취사선택 = 빌드 결정.
- **소모성 아이템 도입(결정)**: 일회용 카드 — 예: "이번 스테이지만 특정 색 확률↑", "즉시 리롤 3회", "다음 팡 점수 ×2", "벽돌 1줄 제거". 상점/드랍으로 획득, 발동하면 소모.

---

## 7. 유물 (Relics) — 초안 목록

카테고리별로 정리. 실제 수치·아이콘·이름은 추후.

### A. 확률 조작
- 🎯 **집중 (등급별로 존재)**: 특정 과일 확률↑. 여러 티어로:
  - 묶음형: "체리+레몬 확률↑" (같은 등급 2종)
  - 단일형: "레몬만 ↑", "체리만 ↑" (핀포인트, 효과 더 큼)
- 🍌 **바나나에 미친 사람**: 바나나 확률 대폭↑ (컨셉 유물, 고점수 올인 빌드의 축)
- 🚫 **봉인**: 특정 색 1개 안 나오게 (덱 압축)
- 🔮 **예지**: 우상단 큐를 2개 더 미리보기

### B. 점수 조작
- 💥 **대량 보너스**: 한 번에 5개 이상 팡 시 점수 ×1.5
- 🔗 **연쇄 광**: 연쇄 크기 보너스 계수 2배
- 🌈 **무지개**: 서로 다른 색을 최근 N턴 안에 터뜨렸으면 배율↑
- ⭐ **고배율 특화**: 배율 1.5 이상 과일 점수 추가 ×2

### C. 자원
- ✋ **지구력**: 스테이지당 최대 터치 +3
- 🔁 **리롤권**: 리롤 3회 지급/충전
- 🪙 **부자**: 스테이지 클리어 코인 +50%

### D. 리스크형 (하이리스크 하이리턴)
- 🌊 **폭풍 스폰**: 터치당 랜덤 스폰 개수 +1 (점수 기회↑, **판이 빨리 차서 죽을 위험↑**)
- 🧱 **벽돌 계약**: 큰 점수 보너스 대신 터치 시 벽돌 랜덤 생성 (판 압박↑)

### E. 맵/영역 (구현 확인됨 ✅)
- 🟩 **명당**: 표시된 칸/영역(중앙·모서리 등)에서 팡 시 점수 ×N
- 🪙 **금맥**: 표시된 칸에서 터뜨리면 동전 드랍
- 🗺️ **영역 확장**: 보너스 존 크기/개수↑

### F. 아이템 강화 (기존 아이템 업그레이드)
- 💣 **폭탄 개조**: 폭탄 폭발 범위 +1 (5×5 → 7×7)
- ↔️ **라인 증폭**: 라인이 인접 2줄까지 청소
- 🌟 **별 확장**: 별이 색 2개를 동시에 정리
- 🐦 **참새 무리**: 참새가 타깃 2개를 쫌
- (아이템 발동 문턱↓ 유물도 가능)

### 특성 (Traits) — 난이도 1단계 업마다 "3개 중 1개" 선택
- 유물과 비슷하지만 **런 내 영구 능력치**. (예: "체리 배율 +0.2", "시작 터치 +2")
- 로그라이트 성장의 뼈대. 유물(상점 구매) vs 특성(레벨업 선택)로 획득 경로 이원화.

---

## 8. 리스크 / 죽음 (둘 다 존재 — 결정)
- **터치 소진 + 목표 미달** → 실패.
- **판이 꽉 참** → 아케이드처럼 **즉사** (스타 러시도 유지). → 남은 터치 압박 + 즉사 위험 **이중 긴장**.
- 리스크형 유물(벽돌 계약, 폭풍 스폰 등)이 "판 채우는 위험"과 "점수 기회"를 교환 → 즉사 리스크가 곧 전략 자원.

---

## 9. 수익화 훅 (지금은 대략만 — 깊이 고민 X)
- 📺 리워드 광고: 실패 시 **부활(터치 +N)** / 상점 **유물 1개 무료 리롤** / 스테이지 시작 시 **보너스 유물**.
- 이 모드가 **리텐션·수익의 핵심** (아케이드는 진입·체험용).
- **모드 자체를 유료화(페이월)는 비추천** — 진입장벽↑, 리텐션↓. 무료로 열고 광고로 버는 게 캐주얼 표준.
- **광고 제거는 별도 빌드 X** → 같은 빌드 안에서 **인앱결제(IAP) "광고 제거"** 논컨슈머블 하나 + 구매 플래그로 처리 (AdMob + `adsRemoved` 체크). 유지보수 편하고 표준 방식.
- (정리: 무료 + 리워드 광고 + 선택적 "광고 제거" IAP. 지금은 이 정도만.)

---

## 10. 밸런스 노브 & 결정 사항
1. **런 종료 방식**: ✅ **무한(하이스코어)** 로 결정.
2. **판 꽉 참 처리**: ✅ **즉사 유지** — 남은 터치 압박 + 즉사 위험 이중 긴장.
3. **코인 획득원**: ✅ **클리어 보상 + 팝 드랍(동전 오브젝트)**. 아케이드 공용 재화화 검토(§4-1).
4. **활성 색 개수**: ✅ **처음부터 7색**. → **판 크기 8×8 → 10×10 검토 중 (내 의견 아래)**.
5. **터치 예산 / 목표**: 기본점수가 한 자리(1~5)라 **목표는 100점 이내**부터. (예: 스테이지1 = 15터치 / 목표 60점) — MVP에서 감으로 잡고 조정.
6. **점수식**: §3-2로 확정(과일 기본점수 × 배율 × 연쇄 × 콤보 × 유물 + 영역보너스).

### 10-4 상세: 판 크기 8×8 vs 10×10 — 내 의견
7색을 **처음부터** 쓰면 8×8(64칸)은 한 색당 평균 ~9칸이라 **같은 색이 인접하게 뭉치기 어려움**(팡 내기 빡셈). 그래서:
- **10×10(100칸) 장점**: 색이 많아도 뭉칠 공간 여유, 영역 보너스·동전·벽돌 배치 공간↑, 빌드 표현 폭↑.
- **10×10 단점**: 폰에서 셀이 작아져 **과일 가독성↓**, 3×3 팡이 판에서 차지하는 비율↓(9/100)라 한 방의 폭발감·연쇄 체감이 옅어짐, 판 채우는 데 더 오래 걸려 **템포 느려짐**.
- **추천**: 아케이드는 **8×8 유지**, 스타 러시만 **9×9 또는 10×10**을 폰에서 두 개 다 만들어보고 감으로 결정. 개인적으론 7색+영역요소 감안하면 **9×9가 균형점**일 수 있음(가독성·템포 덜 해치면서 공간 확보). N을 모드별 변수로 두면 실험 쉬움. → **플레이 테스트로 확정**.

---

## 11. 구현 단계 (재미부터 검증)
- **0단계 (기반)**: `colorWeight[]` + `pickColor()` — 스폰/큐 전 지점 교체. 동작 동일.
- **1단계 (MVP·재미검증)**: 과일 배율 + 단일 목표("N터치 안에 X점") + 승/패 화면. *유물 없이* 루프만. 폰에서 재미 판정.
- **2단계 (경제+유물)**: 코인 정산 + 간단 상점 + 스타터 유물 4~5종(A/B/C/D 각 1).
- **3단계 (특성 선택지 + 유물 확장 + 시너지 + 광고 연동 + 폴리시)**.

> 진행: 문서 확정 → 0·1단계 구현 → 폰 반복 → 2단계 …

---

## 12. 데이터 구조 스케치 (구현 참고)
```js
// 런 상태
run = {
  stage: 1, quota: 300, touchesLeft: 15, score: 0, coins: 0,
  relics: [],            // 보유 유물 id 목록
  colorWeight: [1,1,1,1,1,1,1],
  fruitMult:   [1,1,1.3,1.3,1.5,1.5,3.0],  // 슬롯별 배율
};
// 유물: { id, name, icon, desc, apply(run){...}, hooks: { onSpawn, onPop, onStageClear } }
```

---

## 13. 정보 UI (Cloverpit 스타일 — 번외 요청)
런 상태를 한눈에 보는 **정보 팝업**들이 배치가 잘 돼야 함. (클로버핏처럼 "지금 내 빌드가 뭘 하는지" 즉시 파악)
1. **내 유물 패널**: 보유 유물 아이콘 나열, 탭하면 이름·효과 설명. 상시 접근 버튼(예: 상단/사이드 아이콘).
2. **과일 도감 팝업**: 7색 각각의 **현재 기본점수 · 배율 · 스폰 확률**을 표로. 유물로 바뀐 값이 실시간 반영(기본값 대비 +표시).
3. **스테이지 정보**: 현재 목표/남은 터치/코인/스테이지 진행도(상단 HUD에 상시).
4. (선택) **소모품/리롤** 접근 UI.
- 레이아웃 원칙: 인게임 방해 최소, 탭으로 열고 닫는 오버레이. 모바일 세로 화면 기준 엄지 닿는 위치 고려.

---

## 부록: 이번 리뷰에서 확정/추가된 것 (2026-09-04)
- 과일 **기본점수(1/2/3/5) + 배율(기본1.0) 분리**, BASE 균일(10) 폐기
- **판 영역 보너스/유물** — 구현 가능 확인, 채택
- **동전 오브젝트** 채택(팝 드랍 + 클리어 보상, 아케이드 공용 재화 검토)
- **소모성 아이템** 채택
- 유물 카테고리에 **E.맵/영역, F.아이템 강화** 추가, 집중=등급별 티어, 바나나 올인·벽돌 계약 리스크 추가
- 죽음 = **터치 소진 + 판 꽉 참 즉사 둘 다**
- 런 = **무한 하이스코어**, **처음부터 7색**, 목표 100점 이내부터
- 수익화 = 무료 + 리워드 광고 + "광고 제거" IAP(같은 빌드, 페이월 X)
- **판 크기 8×8 vs 9×9 vs 10×10**은 플레이 테스트로 확정 (내 추천: 스타 러시 9×9 검토)
- **정보 UI 팝업**(유물/과일 도감/스테이지) 요구사항 추가
